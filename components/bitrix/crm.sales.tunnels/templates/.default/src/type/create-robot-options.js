type CreateRobotOptions = {
	from: {
		category: string | number,
		stage: string | number,
	},
	to: {
		category: string | number,
		stage: string | number,
	},
};

export default CreateRobotOptions;
