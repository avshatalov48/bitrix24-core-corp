import {CopilotAgreementPopup} from '../../src/copilot-agreement-popup';

describe('CopilotAgreementPopup', () => {
	it('Should be a function', () => {
		assert(typeof CopilotAgreementPopup === 'function');
	});
});