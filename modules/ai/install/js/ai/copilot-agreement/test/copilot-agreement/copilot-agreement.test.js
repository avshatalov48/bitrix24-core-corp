import { CopilotAgreement } from '../../src/copilot-agreement';

describe('CopilotAgreementPopup', () => {
	it('Should be a function', () => {
		assert(typeof CopilotAgreement === 'function');
	});
});
