import {CopilotTextController} from '../../src/copilot-text-controller';

describe('CopilotTextController', () => {
	it('Should be a function', () => {
		assert(typeof CopilotTextController === 'function');
	});
});