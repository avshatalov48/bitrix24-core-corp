import {CopilotImageController} from '../../src/copilot-image-controller';

describe('CopilotImageController', () => {
	it('Should be a function', () => {
		assert(typeof CopilotImageController === 'function');
	});
});