import {ConfirmPagePublic} from '../../src/confirm-page-public';

describe('ConfirmPagePublic', () => {
	it('Should be a function', () => {
		assert(typeof ConfirmPagePublic === 'function');
	});
});
