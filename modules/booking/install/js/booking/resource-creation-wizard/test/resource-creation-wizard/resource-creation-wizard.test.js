import { ResourceCreationWizard } from '../../src/resource-creation-wizard';

describe('ResourceCreationWizard', () => {
	it('Should be a function', () => {
		assert(typeof ResourceCreationWizard === 'function');
	});
});
