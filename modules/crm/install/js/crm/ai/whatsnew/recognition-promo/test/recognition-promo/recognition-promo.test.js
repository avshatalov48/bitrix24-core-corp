import { Type } from 'main.core';
import { RecognitionPromo } from '../../src/recognition-promo';

describe('RecognitionPromo', () => {
	it('Should be a function', () => {
		assert(Type.isFunction(RecognitionPromo));
	});
});
