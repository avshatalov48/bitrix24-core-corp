export type ItemSelectorOptions = {
	id?: string,
	target: HTMLElement,
	valuesList: Array,
	selectedValues: Array,
	readonlyMode?: boolean,
	compactMode?: boolean,
	icon?: string,
	htmlItemCallback?: Function,
	multiple?: boolean,
};
