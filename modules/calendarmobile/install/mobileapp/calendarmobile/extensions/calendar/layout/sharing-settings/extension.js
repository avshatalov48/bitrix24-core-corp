/**
 * @module calendar/layout/sharing-settings
 */
jn.define('calendar/layout/sharing-settings', (require, exports, module) => {
	const { SharingSettings } = require('calendar/layout/sharing-settings/settings');

	module.exports = { SharingSettings };
});
